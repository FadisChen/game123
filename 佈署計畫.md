# 佈署計畫

目標：尾牙當天讓約 60 人透過手機瀏覽器連線遊玩，平常維持個人測試用途。

## 架構決策

**全端（前端靜態頁面 + 後端 Express/Socket.IO）合併部署在同一個 Render.com 服務上**，維持目前程式碼已經在用的同源架構，不需要拆分成兩個服務。

| 部分 | 內容 | 選定方案 |
| --- | --- | --- |
| 前端 + 後端 | `client/dist`（靜態檔） 由 `server`（Express + Socket.IO）同源伺服 | Render.com 免費方案 |

**為什麼不拆前後端**：曾考慮前端上 GitHub Pages、後端上 Render 的分離架構，但那需要改 `SocketClient.ts` 指定後端網址、處理 GitHub Pages 子路徑（`base`）、調整 QR Code 網址與 `/join/:code` 路徑改寫等多處程式碼。全部放同一個 Render 服務可以完全沿用現有的同源架構（`server/src/index.ts` 已經用 `express.static` + `/join/:code`、`/host` 路由改寫伺服 `client/dist`），**不需要修改任何程式碼**，佈署與除錯也更單純。

**為什麼不能全部丟 Google 免費空間**：Google 沒有適合的免費方案能跑常駐 Node.js + WebSocket 伺服器。Cloud Run 雖支援 WebSocket，但預設會縮到 0 個實例，要保持常駐（`min-instances=1`）需綁信用卡且可能超出免費額度，設定門檻也較高。

**為什麼選 Render 而非 Fly.io**：Render 免費方案不需要信用卡即可註冊，設定較單純；缺點是閒置一段時間會休眠，下次連線要等它醒來（尾牙當天可提前手動連線熱機解決）。60 人規模在兩者的免費額度內都綽綽有餘。

**取捨**：全端合一代表前端靜態頁面也會跟著後端一起休眠喚醒（分離架構下 GitHub Pages 前端不會睡著）。尾牙前提早熱機即可，可接受。

## 現況：單一 origin 部署，程式碼不需調整

[server/src/index.ts](server/src/index.ts) 用同一個 Express 伺服器同時做：

1. `app.use(express.static(CLIENT_DIST))` + `/`、`/host`、`/join/:code` 三個路由，伺服器端路由改寫回傳對應的 HTML。
2. Socket.IO 伺服器（`cors: { origin: "*" }`）。

[client/src/net/SocketClient.ts](client/src/net/SocketClient.ts) 的 `io({ transports: ["websocket"] })` 沒有指定網址，代表連到「目前頁面同源」——在 Render 單一服務架構下這正好符合需求，不需要改成讀取環境變數。

`client/vite.config.ts` 也不需要設定 `base`，因為整個 app 會部署在 Render 網域的根路徑，不像 GitHub Pages 專案頁面會有 `/repo-name/` 子路徑。

## 部署步驟

1. **確認 Render 設定**（已提供 [render.yaml](render.yaml)，可用 Render 的 Blueprint 功能一鍵套用，或手動在後台設定）：
   - Build command：`npm install && npm run build`（依序建置 server 型別檢查與 client 靜態檔到 `client/dist`）
   - Start command：`npm run start -w server`
   - Render 會自動注入 `PORT` 環境變數，[server/src/index.ts:14](server/src/index.ts#L14) 已經讀取 `process.env.PORT ?? 3001`，不用額外設定。
2. **在 Render 建立服務**
   - 前往 [Render Dashboard](https://dashboard.render.com/)，New → Blueprint（會讀取 repo 根目錄的 `render.yaml`），或 New → Web Service 手動貼上述指令。
   - 連結 GitHub repo `FadisChen/game123`，選擇要部署的分支。
   - 部署完成後會拿到一個網址，例如 `https://game123.onrender.com`，這就是主辦方與玩家都會用到的網址。
3. **尾牙當天前置檢查**
   - 提前 10～15 分鐘手動連線 Render 網址熱機，避免免費方案休眠造成第一批玩家連線延遲。
   - 主辦方開啟 `https://<render網址>/host` 建立房間，用手機實測 QR Code 掃描 → 加入房間 → 移動 → 鬼回頭判定的完整流程。
   - 用 `scripts/` 內的壓力測試腳本（若有）模擬多人連線，確認 Render 免費方案的資源足夠應付 60 人同時在線。

## 待辦追蹤

- [x] 確認架構決策：全端合一部署在 Render，不需拆分前後端
- [x] 新增 `render.yaml`
- [ ] 在 Render Dashboard 建立服務（Blueprint 或手動設定）並取得正式網址
- [ ] 尾牙前一天完整跑一次端對端測試（手機掃 QR Code）
- [ ] 尾牙當天提前熱機
