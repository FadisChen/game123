# 佈署計畫

目標：尾牙當天讓約 60 人透過手機瀏覽器連線遊玩，平常維持個人測試用途。目前專案仍在開發中，此文件先記錄部署架構與待辦事項，**尚未動手修改程式碼**。

## 架構決策

前端（靜態頁面）與後端（Socket.IO 常駐連線伺服器）分開部署：

| 部分 | 內容 | 選定方案 |
| --- | --- | --- |
| 前端 | `client/dist`（`player.html` + `host.html`） | GitHub Pages |
| 後端 | `server`（Express + Socket.IO） | Render.com 免費方案（備選：Fly.io） |

**為什麼不能全部丟 Google 免費空間**：Google 沒有適合的免費方案能跑常駐 Node.js + WebSocket 伺服器。Cloud Run 雖支援 WebSocket，但預設會縮到 0 個實例，要保持常駐（`min-instances=1`）需綁信用卡且可能超出免費額度，設定門檻也較高。

**為什麼選 Render 而非 Fly.io**：Render 免費方案不需要信用卡即可註冊，設定較單純；缺點是閒置一段時間會休眠，下次連線要等它醒來（尾牙當天可提前手動連線熱機解決）。60 人規模在兩者的免費額度內都綽綽有餘。

## 現況：目前是單一 origin 部署

目前 [server/src/index.ts](server/src/index.ts) 是用同一個 Express 伺服器同時做兩件事：

1. `app.use(express.static(CLIENT_DIST))` + `/`、`/host`、`/join/:code` 三個路由，伺服器端路由改寫回傳對應的 HTML。
2. Socket.IO 伺服器。

前後端分離部署後，這個「同源」假設會被打破，需要調整。

## 需要的程式碼調整（部署前才動手）

### 1. `/join/:code` 與 `/host` 路徑改寫

GitHub Pages 是純靜態空間，沒有伺服器端動態路由改寫能力。

- 方案 A（推薦，改動最小）：在 `client/` 建置輸出的根目錄放一份 `404.html`，內容與 `player.html` 相同。GitHub Pages 對於未知路徑會回傳 `404.html`，網址列仍保留 `/join/CODE`，前端 JS 再從 `location.pathname` 解析房號即可，不需改協定。
- 方案 B：把連結格式改成 `?code=CODE` 這種 query string，完全避開路徑改寫需求，但需要同步改動 QR Code 產生邏輯（[HostController.ts:53](client/src/host/HostController.ts#L53)）與加入房間頁面解析房號的地方。

`/host` 則直接請主辦方使用 `/host.html`，或同樣用方案 A 的 fallback 處理。

### 2. Socket.IO 連線位址

[client/src/net/SocketClient.ts:53](client/src/net/SocketClient.ts#L53) 目前是：

```ts
this.socket = io({ transports: ["websocket"] });
```

沒有指定伺服器網址，代表連到「目前頁面同源」。前後端分離後必須改成指定後端網址，例如透過 Vite 環境變數：

```ts
this.socket = io(import.meta.env.VITE_SERVER_URL, { transports: ["websocket"] });
```

本機開發（同源，走 vite proxy）與正式環境（跨 origin，指向 Render 網址）各自用 `.env` / `.env.production` 區分。

### 3. CORS

[server/src/index.ts](server/src/index.ts) 目前 `new Server(httpServer, { cors: { origin: "*" } })` 已經是全開放，跨 origin 不需要額外調整。正式上線後可考慮收斂成只允許 GitHub Pages 網域，非必要但較安全。

### 4. QR Code 網址

[HostController.ts:53](client/src/host/HostController.ts#L53) 用 `location.origin` 組 QR Code 連結，這部分不用改，搬到 GitHub Pages 後會自動變成正確網址。

## 部署步驟（規劃中，尾牙前執行）

1. **後端上 Render**
   - 新增 `render.yaml` 或直接在 Render 後台設定：Build command `npm install && npm run build -w server`，Start command `npm run start -w server`（或對應 workspace 指令）。
   - 記下 Render 配發的網址（如 `https://xxx.onrender.com`），供前端 `VITE_SERVER_URL` 使用。
2. **前端上 GitHub Pages**
   - 完成上述路徑改寫（404.html fallback）與 `VITE_SERVER_URL` 調整。
   - 用 GitHub Actions 或手動 `vite build` 後把 `client/dist` 推到 `gh-pages` 分支（或用 `actions/deploy-pages`）。
   - 確認 `client/vite.config.ts` 的 `base` 設定符合 GitHub Pages 的子路徑（若使用 `https://<user>.github.io/<repo>/` 而非自訂網域，需設定 `base: "/<repo>/"`）。
3. **尾牙當天前置檢查**
   - 提前 10～15 分鐘手動連線 Render 後端熱機，避免免費方案休眠造成第一批玩家連線延遲。
   - 用手機實測 QR Code 掃描 → 加入房間 → 移動 → 鬼回頭判定的完整流程，確認跨 origin 後 Socket.IO 連線與延遲正常。
   - 用 `scripts/` 內的壓力測試腳本（若有）模擬多人連線，確認 Render 免費方案的資源足夠應付 60 人同時在線。

## 待辦追蹤

- [ ] `client/src/net/SocketClient.ts`：改為讀取 `VITE_SERVER_URL`
- [ ] `client/` 建置輸出加入 `404.html` fallback（或改用 query string 方案）
- [ ] 新增 `.env.production`（`VITE_SERVER_URL` 指向 Render 網址）
- [ ] Render 後台建立服務並取得正式網址
- [ ] GitHub Pages 設定與 `vite.config.ts` 的 `base` 調整
- [ ] 尾牙前一天完整跑一次端對端測試（含跨 origin）
